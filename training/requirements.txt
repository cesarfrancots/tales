# Pinned to a known-compatible set for the LoRA SFT pipeline. If you bump these,
# expect SFTConfig/SFTTrainer argument churn (trl renames things between minors).
transformers==4.44.2
datasets==2.21.0
peft==0.12.0
trl==0.9.6
accelerate==0.33.0
torch>=2.3
# Optional: 4-bit base loading on small GPUs.
bitsandbytes==0.43.3
