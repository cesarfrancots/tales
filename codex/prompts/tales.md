# /tales — open the Tales terminal

Run exactly this shell command and nothing else, then stop:

```
tales open --connect codex "$ARGUMENTS"
```

It opens the interactive Tales terminal in a new window with Codex pre-connected
(add Claude Code / Open Code there, type a task, plan together, then pick who
executes). Do not inspect anything, reason, or explain — just run the one command.
